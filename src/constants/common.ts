import { RouterPaths } from 'types';

export const ROUTER_PATHS: RouterPaths = {
  main: '/',
  authorization: 'auth',
  textbook: 'textbook',
  sprintGame: 'sprint',
  audioCallGame: 'audiocall',
};
