import { ROUTER_PATHS, LOCAL_STORAGE_KEYS } from './common';
import { BASE_URL, ENDPOINTS } from './api';
import {
  MIN_NAME_LENGTH,
  MAX_NAME_LENGTH,
  MIN_PASSWORD_LENGTH,
  MESSAGE_REQUIRED,
  SERVER_ERROR,
} from './authorization';

export {
  ROUTER_PATHS,
  BASE_URL,
  ENDPOINTS,
  LOCAL_STORAGE_KEYS,
  MIN_NAME_LENGTH,
  MAX_NAME_LENGTH,
  MIN_PASSWORD_LENGTH,
  MESSAGE_REQUIRED,
  SERVER_ERROR,
};
