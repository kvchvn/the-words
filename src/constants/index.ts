import {
  BASE_MEDIA_URL,
  BASE_URL,
  ENDPOINTS,
  SERVER_ERROR,
  TAG_ID,
  TOKEN_EXPIRED_ERROR,
} from './api';
import {
  MAX_NAME_LENGTH,
  MESSAGE_REQUIRED,
  MIN_NAME_LENGTH,
  MIN_PASSWORD_LENGTH,
} from './authorization';
import { FROM_MAIN, FROM_TEXTBOOK, LOCAL_STORAGE_KEYS, ROUTER_PATHS } from './common';
import {
  AUDIOCALL_ANSWERS_ARRAY_LENGTH,
  AUDIOCALL_DO_NOT_KNOW_ANSWER,
  DEFAULT_STATISTICS,
  GAME_ROUND_TIME,
  GAME_TYPES,
  MAX_PROGRESS_TO_COMBO,
  MEANING_ANSWERS_AMOUNT,
  MIN_COMBO,
  MIN_PROGRESS_TO_COMBO,
  ROUND_SCORE,
  STARTED_WORD_INDEX,
} from './games';
import {
  DELTA,
  EASY_WORD,
  HARD_WORD,
  MAX_GROUP,
  MAX_GROUP_FOR_GUESTS,
  MAX_GROUP_FOR_USERS,
  MAX_PAGE,
  MIN_GROUP,
  MIN_PAGE,
  NOT_EASY_WORD,
  WORD_WITHOUT_DIFFICULTY,
  WORDS_PER_PAGE,
} from './words';

export {
  ROUTER_PATHS,
  BASE_URL,
  ENDPOINTS,
  LOCAL_STORAGE_KEYS,
  MIN_NAME_LENGTH,
  MAX_NAME_LENGTH,
  MIN_PASSWORD_LENGTH,
  MESSAGE_REQUIRED,
  SERVER_ERROR,
  MIN_GROUP,
  MIN_PAGE,
  MAX_GROUP,
  MAX_PAGE,
  DELTA,
  HARD_WORD,
  EASY_WORD,
  TOKEN_EXPIRED_ERROR,
  MAX_GROUP_FOR_USERS,
  MAX_GROUP_FOR_GUESTS,
  WORDS_PER_PAGE,
  FROM_MAIN,
  FROM_TEXTBOOK,
  NOT_EASY_WORD,
  GAME_ROUND_TIME,
  STARTED_WORD_INDEX,
  GAME_TYPES,
  WORD_WITHOUT_DIFFICULTY,
  MEANING_ANSWERS_AMOUNT,
  TAG_ID,
  AUDIOCALL_ANSWERS_ARRAY_LENGTH,
  AUDIOCALL_DO_NOT_KNOW_ANSWER,
  BASE_MEDIA_URL,
  MAX_PROGRESS_TO_COMBO,
  MIN_PROGRESS_TO_COMBO,
  MIN_COMBO,
  ROUND_SCORE,
  DEFAULT_STATISTICS,
};
