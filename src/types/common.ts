export interface Theme {
  fontFamily: {
    primary: string;
  };
  color: {
    blue: string;
    darkblue: string;
    gray: string;
    white: string;
    coral: string;
  };
}

export interface RouterPaths {
  main: string;
  authorization: string;
  textbook: string;
  sprintGame: string;
  audioCallGame: string;
  gameResults: string;
  gameWelcome: string;
  statistic: string;
}
