export interface Theme {
  fontFamily: {
    primary: string;
  };
}

export interface RouterPaths {
  main: string;
  authorization: string;
  textbook: string;
  sprintGame: string;
  audiocallGame: string;
}
